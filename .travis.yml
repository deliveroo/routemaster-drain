language: ruby
cache: bundler
services:
  - redis-server
rvm:
  - 2.2.5
  - 2.3.1
gemfile:
  - gemfiles/3.2.14.gemfile
  - gemfiles/4.0.0.gemfile
  - gemfiles/5.0.0.gemfile
script:
- bundle exec rspec
- bundle exec codeclimate-test-reporter
